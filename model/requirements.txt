# Training + export dependencies for model/ (Colab or local GPU).
# Kept SEPARATE from backend/requirements.txt — the backend never trains and
# never imports TensorFlow. Install with:  pip install -r model/requirements.txt
#
# TensorFlow ships preinstalled on Colab, so this install is basically a no-op
# for TF there (and needs NO runtime restart). Export uses TF's built-in TFLite
# converter — no fragile third-party ONNX tooling, so it tracks whatever TF
# version Colab provides.

tensorflow>=2.16   # preinstalled on Colab; pinned floor for local installs
kagglehub>=0.3     # dataset download
