fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
requests>=2.31

# Phase 2 — persistence, auth, images
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9   # PostgreSQL driver (SQLite needs no driver)
bcrypt>=4.1            # password hashing
pyjwt>=2.8             # JWT access tokens
email-validator>=2.0   # pydantic EmailStr
Pillow>=10.0          # EXIF stripping of meal images
tzdata>=2024.1         # IANA timezone data (needed on Windows)

# Real model backend (MODEL_BACKEND=tflite). Optional — the stub needs neither.
# Lightweight TFLite runtime; no full TensorFlow on the server.
ai-edge-litert>=1.0   # serves model/versions/*/model.tflite
numpy>=1.24           # image array for inference

# tests
pytest>=8.0
httpx>=0.27
